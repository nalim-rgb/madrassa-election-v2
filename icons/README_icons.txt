How to add custom icons to candidates:
1. Place any icon files here (e.g., "mug.png", "car.jpg", "pen.svg").
2. Then, inside `candidates.js`, set the candidate icon property to the path like `"icons/mug.png"`.

You can also just use Emojis directly in candidates.js like `"🍎"`. No file needed in that case!
